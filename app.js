const Math = require('./math');

const a = 22;
const b = 11;

const sum = Math.add(a, b);
console.log(`Hello Peter, the sum of ${a} and ${b} is ${sum}`);

const difference = Math.subtract(a, b);
console.log(`Hello Peter, the difference of ${a} and ${b} is ${difference}`);